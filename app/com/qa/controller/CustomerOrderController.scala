package com.qa.controller

import play.api.mvc._

/**
  * Created by cboucher on 19/11/2015.
  */
class CustomerOrderController extends Controller {

  /*private val customerOrderForm = Form(
    tuple(
      "idCustomer" -> nonEmptyText,
      "idAddress" -> nonEmptyText
    ))
  val (idCustomer, idAddress) = customerOrderForm.bindFromRequest.get
  QueryLoader*/
}
